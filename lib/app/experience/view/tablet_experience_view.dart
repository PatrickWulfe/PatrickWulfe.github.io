import 'package:flutter/material.dart';

class TabletExperienceView extends StatelessWidget {
  const TabletExperienceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
