const zoomDuration = Duration(milliseconds: 500);
const rollDuration = Duration(milliseconds: 1000);
const toggleDuration = Duration(milliseconds: 1000);
const minDragStartEdge = 200;
const maxDragStartEdge = 2000;
const maxSlide = 350;
