const zoomDuration = Duration(milliseconds: 250);
const rollDuration = Duration(milliseconds: 500);
const toggleDuration = Duration(milliseconds: 600);
const minDragStartEdge = 200;
const maxDragStartEdge = 1000;
const maxSlide = 350;
