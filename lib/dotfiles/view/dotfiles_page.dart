import 'package:flutter/material.dart';

class DotfilesPage extends StatelessWidget {
  const DotfilesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DotfilesView();
  }
}

class DotfilesView extends StatelessWidget {
  const DotfilesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}

class AndroidStudioTile extends StatelessWidget {
  const AndroidStudioTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class VSCodeTile extends StatelessWidget {
  const VSCodeTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
